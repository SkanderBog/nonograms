{
  "name": "nonograms",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Verified nonogram puzzles — generate in the browser or from the CLI",
  "scripts": {
    "gen": "node generator.js",
    "build": "node build.js",
    "test": "node build.js --check && node tests.js"
  },
  "engines": {
    "node": ">=18"
  }
}
